# name: Space & Science Brief

# on:
#   schedule:
#     # 10:30 PM IST = 5:00 PM UTC
#     - cron: "0 17 * * *"
#   workflow_dispatch:

# jobs:
#   generate-space-report:
#     runs-on: ubuntu-latest
#     timeout-minutes: 30
#     permissions:
#       contents: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python 3.11
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       - name: Install dependencies
#         run: pip install -r requirements.txt

#       - name: Run Space & Science Brief
#         env:
#           OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#           NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
#           NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
#           EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
#           EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
#           EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
#           SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
#           SMTP_PORT: ${{ secrets.SMTP_PORT }}
#           NASA_API_KEY: ${{ secrets.NASA_API_KEY }}
#         run: python scripts/run_space.py

#       - name: Upload report artifact
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: space-report-${{ github.run_id }}
#           path: reports/space_*.md
#           retention-days: 30
#           if-no-files-found: warn

#       - name: Commit report to repository
#         if: success()
#         run: |
#           git config --local user.email "github-actions[bot]@users.noreply.github.com"
#           git config --local user.name "github-actions[bot]"
#           git add reports/space_*.md || true
#           git diff --staged --quiet || git commit -m "feat: space science brief $(date +'%Y-%m-%d')"
#           git push || true
